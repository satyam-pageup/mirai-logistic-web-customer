import { query } from "@angular/animations";

export const appRoutes = {
    login: "login",
    dashboard: 'dashboard',

    order: {
        base: 'order-list',
        summary:"order-summary",
    },

    query:{
        base:'query'
    }
}